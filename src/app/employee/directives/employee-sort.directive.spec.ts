import { EmployeeSortDirective } from './employee-sort.directive';

describe('EmployeeSortDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeeSortDirective();
    expect(directive).toBeTruthy();
  });
});
